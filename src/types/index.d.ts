export interface Hero {
    title: string;
    subtitle?: string;
    backgroundImage?: { url: string };
  }
  
  export interface Service {
    title: string;
    description: string;
    icon?: { url: string };
  }
